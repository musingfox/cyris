# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/

# Virtual environment
.venv/
venv/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment and secrets
.env
.env.*
!.env.example
*.pem
cookie*.txt

# OS
.DS_Store
Thumbs.db

# Runtime config (copy from *.example)
cyris.toml
sources.yaml

# Agent vault runtime data
agent-vault/daily/
agent-vault/articles/
agent-vault/html/
agent-vault/usage.jsonl
agent-vault/tracking.yaml
# vote-similarity vector cache — regenerable, and large
agent-vault/embeddings.json
# buffer-vs-Miniflux parity log + its launchd stdio; both go once Miniflux is retired
agent-vault/source-parity.jsonl
agent-vault/source-parity.out
agent-vault/source-parity.err

# Docker deployment host output (CYRIS_VAULT_HOST_PATH default)
/vault/

# Claude Code
.claude/settings.local.json
.claude/*.local.md

# Obsidian PM (local obw:pm config — per-developer, not shipped)
.obsidian.yaml
.obsidian-pm.yaml

# Test / coverage
.coverage
htmlcov/
.pytest_cache/

# JS deps for the defuddle shim
node_modules/
.spiral/
