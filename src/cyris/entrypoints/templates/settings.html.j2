<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cyris Settings</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Geist:wght@300;400;500;600;700&family=Geist+Mono:wght@400;500&family=Instrument+Serif:ital@0;1&display=swap">
  <link rel="stylesheet" href="/static/style.css">
  <style>
    /* This page's own layout. Every shared control and role comes from
       style.css; a rule here that another page also needs belongs there. */
    body { font-size: calc(16px * var(--type-scale)); line-height: 1.6; min-height: 100vh; }
    a { color: inherit; }
    button, input, select { font: inherit; color: inherit; }
    [hidden] { display: none !important; }
    .page { max-width: 1240px; margin: 0 auto; padding: var(--s-12) var(--s-4) var(--s-20); }
    .page-head { display: grid; gap: var(--s-4); margin-bottom: var(--s-12); }
    .settings { display: grid; grid-template-columns: 220px minmax(0, 1fr); gap: var(--s-12); align-items: start; }
    .settings-nav { position: sticky; top: calc(var(--bar-h) + var(--s-6)); display: grid; border-left: 1px solid var(--border); }
    .settings-nav a { display: flex; align-items: center; justify-content: space-between; padding: var(--s-2) var(--s-4); margin-left: -1px; text-decoration: none; border-left: 2px solid transparent; transition: color var(--t-fast), border-color var(--t-fast); }
    .settings-nav a:hover { color: var(--text); }
    .settings-nav a[aria-current="page"] { color: var(--accent); border-left-color: var(--accent); }
    .dirty-dot { width: 6px; height: 6px; background: var(--accent); border-radius: 50%; visibility: hidden; }
    .settings-nav a.dirty .dirty-dot { visibility: visible; }
    .tab-head { display: grid; gap: var(--s-2); margin-bottom: var(--s-8); max-width: var(--measure); }
    .form { display: grid; gap: var(--s-6); max-width: var(--measure); }
    .field-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); gap: var(--s-4); }
    .input.narrow { width: 96px; }
    .key-missing { color: var(--warn); }
    .actions-line { display: flex; align-items: center; gap: var(--s-4); flex-wrap: wrap; }
    @media (max-width: 720px) {
      .page { padding-top: var(--s-8); }
      .settings { grid-template-columns: minmax(0, 1fr); gap: var(--s-6); }
      .settings-nav { position: static; display: flex; overflow-x: auto; border-left: 0; border-bottom: 1px solid var(--border); }
      .settings-nav a { border-left: 0; margin-left: 0; border-bottom: 2px solid transparent; margin-bottom: -1px; white-space: nowrap; gap: var(--s-2); }
      .settings-nav a[aria-current="page"] { border-bottom-color: var(--accent); }
    }
  </style>
</head>
<body>
{% with current="settings" %}{% include "_site_bar.html.j2" %}{% endwith %}
  <main class="page">
    <div class="page-head">
      <span class="label">Settings</span>
      <h1 class="display">How cyris <em>runs</em></h1>
    </div>

    <div class="settings">
      <nav class="settings-nav" aria-label="Settings sections">
        <a class="label" href="#model" data-tab="model">Model <span class="dirty-dot"></span></a>
        <a class="label" href="#digest" data-tab="digest">Digest <span class="dirty-dot"></span></a>
        <a class="label" href="#notifications" data-tab="notifications">Notifications <span class="dirty-dot"></span></a>
        <a class="label" href="#sources" data-tab="sources">Sources <span class="dirty-dot"></span></a>
      </nav>

      <div>
        <form class="tab" data-tab="model" id="form" hidden>
          <div class="tab-head">
            <h2 class="heading">Model</h2>
            <p class="small">The LLM that scores, filters and summarizes. A change applies from the next run.</p>
          </div>
          <div class="form">
            <div class="field">
              <span class="label" id="provider-label">Provider</span>
              <div class="choices" id="providers" role="radiogroup" aria-labelledby="provider-label"></div>
            </div>
            <div class="field">
              <label class="label" for="model-input">Model</label>
              <input class="input" id="model-input" type="text" autocomplete="off">
              <p class="small" id="model-hint"></p>
            </div>
            <div class="actions-line">
              <button class="btn primary" type="submit" id="save" disabled>Save</button>
              <div class="notice" id="result" hidden></div>
            </div>
          </div>
        </form>

        <form class="tab" data-tab="digest" id="digest-form" hidden>
          <div class="tab-head">
            <h2 class="heading">Digest</h2>
            <p class="small">When issues are published and how the page is laid out.</p>
          </div>
          <div class="form">
            <div class="field">
              <span class="label">Publish hours</span>
              <div class="field-row">
                <div class="field"><label class="small" for="morning">Morning</label><input class="input narrow" id="morning" type="number" min="0" max="23" autocomplete="off"></div>
                <div class="field"><label class="small" for="evening">Evening</label><input class="input narrow" id="evening" type="number" min="0" max="23" autocomplete="off"></div>
              </div>
              <details class="more"><summary>More</summary><p class="small">Two whole hours, in the timezone from <code>[general]</code>. The tick is hourly, so half hours are refused rather than quietly rounded down.</p></details>
            </div>
            <div class="field">
              <label class="label" for="max-featured">Featured sections</label>
              <input class="input narrow" id="max-featured" type="number" min="1" max="20" autocomplete="off">
              <p class="small">How many sections lead the page before the rest.</p>
              <details class="more"><summary>More</summary><p class="small">Your call, not a measured number — the digest reads differently at 3 than at 8. It takes effect on the next digest.</p></details>
            </div>
            <div class="actions-line">
              <button class="btn primary" type="submit" id="save-digest" disabled>Save</button>
              <div class="notice" id="digest-result" hidden></div>
            </div>
          </div>
        </form>

        <form class="tab" data-tab="notifications" id="notify-form" action="/api/settings/notify" hidden>
          <div class="tab-head">
            <h2 class="heading">Notifications</h2>
            <p class="small">Where a finished issue is announced.</p>
          </div>
          <div class="form">
            <div class="field">
              <label class="label" for="discord-webhook">Discord webhook</label>
              <input class="input" id="discord-webhook" type="text" placeholder="https://discord.com/api/webhooks/..." autocomplete="off">
              <p class="small">To change it, paste the whole URL. It is saved only after Discord confirms it exists.</p>
              <details class="more"><summary>More</summary><p class="small">A stored webhook shows with its token masked, and the masked value cannot be resubmitted. When empty here, a run still uses the <code>[notify]</code> value in cyris.toml, then <code>CYRIS_DISCORD_WEBHOOK_URL</code>. This page cannot turn notifications off: to stop them, remove that Worker secret and any <code>[notify]</code> value.</p></details>
            </div>
            <div class="actions-line">
              <button class="btn primary" type="submit" id="save-notify" disabled>Save</button>
              <div class="notice" id="notify-result" hidden></div>
            </div>
          </div>
        </form>

        <div class="tab" data-tab="sources" hidden>
          <div class="tab-head">
            <h2 class="heading">Sources</h2>
            <p class="small">Feeds and newsletter senders. A change applies from the next run.</p>
            <p class="small" id="sources-origin"></p>
          </div>
          <div id="src-list"></div>
          <form id="src-form" class="form">
            <div class="field-row">
              <div class="field"><label class="label" for="src-name">Name</label><input class="input" id="src-name" type="text" autocomplete="off"></div>
              <div class="field"><label class="label" for="src-type">Type</label>
                <select class="select" id="src-type"><option value="rss">rss</option><option value="newsletter">newsletter</option></select></div>
              <div class="field"><label class="label" for="src-tier">Tier</label>
                <select class="select" id="src-tier"><option value="filter">filter</option><option value="summarize">summarize</option><option value="fan">fan</option></select></div>
            </div>
            <div class="field"><label class="label" for="src-url">Feed URL</label><input class="input" id="src-url" type="text" autocomplete="off"></div>
            <div class="field"><label class="label" for="src-email">Sender match</label><input class="input" id="src-email" type="text" placeholder="from:hi@letter.com" autocomplete="off">
              <p class="small">Maps a sender to this source. Email routing itself stays in the Cloudflare dashboard.</p></div>
            <div class="field"><label class="label" for="src-homepage">Homepage</label><input class="input" id="src-homepage" type="text" autocomplete="off"></div>
            <div class="field"><label class="label" for="src-tags">Tags</label><input class="input" id="src-tags" type="text" placeholder="comma separated" autocomplete="off">
              <p class="small">The name is the key: saving an existing name edits that source.</p></div>
            <div class="actions-line">
              <button class="btn primary" type="submit" id="save-src">Save source</button>
              <div class="notice" id="src-result" hidden></div>
            </div>
          </form>
        </div>
      </div>
    </div>
  </main>

<script src="/static/settings.js"></script>
</body>
</html>
