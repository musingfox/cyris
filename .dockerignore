.git
.venv
node_modules
__pycache__
*.pyc
.pytest_cache
.ruff_cache
# secrets stay out of the image; tokens are read from the environment
.env
tracking.yaml
# cyris.toml and sources.yaml are gitignored — do not COPY them. The image bakes
# sources.example.yaml as /app/sources.yaml; compose bind-mounts over that path.
# Grade-B identity (worker URLs, Pages project, D1 id) comes from the environment.
cyris.toml
sources.yaml
# runtime data (volume-mounted)
agent-vault
vault
# not needed in image
tests
docs
workers
*.md
!README.md
