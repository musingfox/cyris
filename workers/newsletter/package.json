{
  "name": "cyris-newsletter",
  "private": true,
  "type": "module",
  "dependencies": {
    "postal-mime": "^2.4.4"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "cloudflare": {
    "bindings": {
      "NEWSLETTER_TOKEN": {
        "description": "**Required.** The bearer this Worker accepts. Use the same value you set as `CYRIS_WORKER_TOKEN` on `cyris-app` — that one secret is what the rss and newsletter Workers both check. Without it every pull answers 401 and email-only sources never reach a digest."
      }
    }
  }
}
